# Syd Salmon's zsh configuration: alias list

# aliases
## global aliases
alias -g fz=';printf "\033c\n";lsd|grep --color=never --invert-match "dr.*"|tail -10' # show 10 most recent files
alias -g iz=';printf "\033c\n";lsd|grep --color=never --invert-match "\..*"|tail -10' # show 10 most recent items
alias -g lsD='ls -aFGhot'  # sort, ascescending, date
alias -g lsd='ls -aFGhort' # sort, descending, DATE
alias -g lsN='ls -aFGho'   # sort, ascescending, name
alias -g lsn='ls -aFGhor'  # sort, descending, NAME
alias -g lsS='ls -aFGhos'  # sort, ascescending, size
alias -g lss='ls -aFGhors' # sort, descending, SIZE
alias -g nv='/Users/syd/Dropbox/nvalt-repo/'
alias -g pbc='|pbcopy'                                               # copy clipboard
alias -g songd='/Users/syd/Dropbox/nvalt-repo/music/songs/'          # song dir
alias -g songo='/Users/syd/Dropbox/nvalt-repo/music/songs/original/' # originals
alias -g songr='/Users/syd/Dropbox/nvalt-repo/music/songs/rock/'     # rock

## simple aliases
alias ....='cd ../../../' # go up three levels
alias ...='cd ../../'     # go up two levels
alias ..='cd ../'         # go up one level
alias {bc,calc}='bc --mathlib --quiet' # calculator
alias brewster='. /Users/syd/Code/misc/Brewster/brewster'
alias bric='brew install --cask'                   # install Homebrew cask (GUI app)
alias brif='brew install'                          # install Homebrew formulae (CLI app)
alias brs='brew search'                            # search Homebrew
alias cls='printf "\033c\n"'                       # clear screen via ^L
alias cntf='find . -type f -maxdepth 1|wc -l'      # files: count FILES in dir
alias cnti='find . -maxdepth 1|wc -l'              # items: count ITEMS in dir
alias cpv='rsync -ah --info=progress2'             # copy, readable xfer stats
alias diff='colordiff -u'                          # compare lines, unify context
alias dirs='dirs -v'                               # list dir stack w/ index number
alias eip='ipconfig getifaddr en0'                 # get IP address for Ethernet
alias ej='. /Users/syd/Code/misc/scripts/ejector'  # eject external media
alias gip='curl -sw \n https://ifconfig.me/ip|tee >(tr -d \n|pbcopy)' # get IP address
alias gitA='git add . && git commit -m "" && gitp' # git add && git commit all
alias gitk='git clone'                             # clone repo to a new directory
alias gitp='git push;printf "\033c\n";git status'  # git push
alias gits='printf "\033c\n";git status'           # get git status
alias grep='grep -i --color=never'                 # set case -[i]nsensitive
alias h='history'
alias ip='curl -sw "\n" https://ifconfig.me/ip|tee >(tr -d "\n"|pbcopy)'
alias j='jobs -l'
alias ls='ls -alh'                                       # remap default ls
alias manzmv='LESS="$LESS +/^ *zmv *\\[" man zshcontrib' # zmv manpage
alias meteo='bash /Users/syd/Code/meteo/meteo.sh'        # show weather
alias misd='/Users/syd/Code/misc ; iz'                   # mnemonic: misc. directory
alias mkdir='mkdir -pv'                                  # create, list dirs, if req'd
alias nvad='/Users/syd/Dropbox/nvalt-repo ; iz'          # mnemonic: nvALT repo dir
alias osa='open /Applications/Signal.app'
alias osk='open /Applications/Sidekick.app'
alias owa='open /Applications/WhatsApp.app'
alias path='printf ${PATH//:/\\n}"\n"'            # show binary directories
alias pbpaste='print "$(pbpaste)"'                # add \n, suppress % (no \n)
alias pcd='printf "$PWD\n"|tee /dev/tty|pbcopy'   # print current dir
alias pd='pushd'                                  # stack dirs, toggle dirs
alias png='ping -c10 1.1.1.1'                     # send 10 packets
alias quorg='ssh syd@quorg.local'                 # connect to local device
alias rand='q=$(( 0 + RANDOM % 10 ));print "$q"'  # get random number >=0<=10
alias rca='vim /Users/syd/.zsh_aliases'           # edit zsh aliases
alias rcf='vim /Users/syd/.zsh_functions'         # edit zsh functions
alias rcp='vim /Users/syd/.zsh_plugins'           # edit zsh plugins
alias rcv='vim /Users/syd/.vimrc'                 # edit vim config
alias rcz='vim /Users/syd/.zshrc'                 # edit zsh config
alias rm='printf "%s\n" "" "@@@--> WARNING <--@@@" "Do no use ${(%):-%F{red}rm${(%):-%f{red}. Use one of:" "" "${(%):-%F{green}|> trh${(%):-%f{green}" "|> sudo rm -Rfd"'
alias scanz='. /Users/syd/Code/misc/scanz/scanz'
alias sl='. "$HOME/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"'
alias smartrg='ssh -oKexAlgorithms=+diffie-hellman-group1-sha1 admin@192.168.1.1'
alias src='. /Users/syd/.zshrc' # source zsh
alias srm='rm -iPv' # @@-> DELETE <-@@ overwrite file 3x, interactive, verbose
alias ssh1='ssh 192.168.1.' # ssh to local device
alias teux='vim /Users/syd/Dropbox/nvalt-repo/teuxdeux.md' # open To Do list
alias trh='trash' # trash item(s)
alias {v,vi}='vim'
alias wip='ipconfig getifaddr en1'
alias wttr='curl https://wttr.in/YKF\?F --silent --max-time 3' # get weatherForecast
alias zcalc='zcalc -f -r4' # floating point, four stacks
alias zcp='zmv -C'         # ~ cp -f (copy, force)
alias zln='zmv -L'         # ~ ln -f (hard link, force)
alias zls='zmv -Ls'        # ~ ln -sf (symbolic link, force)
alias zmN='zmv -n '\''(*) (*)'\'' '\''${(L)1}-${(L)2}'\''' # change space to dash, dry run, no exec
alias zmn='zmv -nwv '\''* *'\'' '\''${(L)f// /-}'\'''      # change space to dash, dry run, no exec, implicit parentheses
alias zmr='!zmv:0 !!:2-$ && cl'                            # rerun last zmv command without -n (rm dry run flag)

### privileged access
if [[ "$UID" -ne 0 ]]; then
    alias dgk='sudo spctl --master-disable # disable Gatekeeper'
    alias egk='sudo spctl --master-enable # enable Gatekeeper'
    alias rms='printf "%s\n" "@@@--> WARNING <--@@@" "Do not use $(tput bold)${(%):-%F{red}rm${(%):-%f}$(tput sgr0)." "Use $(tput bold)${(%):-%F{green}|> trh <|${(%):-%f}$(tput sgr0) or sudo rm -Rfd"'
    alias sd='sudo -b shutdown -h +1 "-> SHUTTING DOWN <-\n->  To abort, type ^c." ; wait 0.5 ; exit'
    alias sr='sudo shutdown -r now "-> REBOOTING <-\n->  To abort, type ^c."'
    alias svi='sudo vi'
    alias topc='sudo htop -s PERCENT_CPU' # sort running processes by CPU%
    alias topm='sudo htop -s PERCENT_MEM' # sort running processes by MEM%
fi

## parametrized aliases
rp() { realpath -m $1|tee /dev/tty|pbcopy } # resolve qualified path: get resolved canonical path
rfd() { awk 'NF>0' $HOME/Downloads/app_purge/$1|while read line ; do sudo rm -Rfd "${line}.md" ; done }
# deleted items listed in app_purge file

### git functions
gita() { git add $1 && git commit -m "$2" } # git add: add & commit to git repo
gitr() { git rm $1 && git commit -m "$2" }  # git delete: delete & commit to git repo
gitd() { git diff $1 }                      # git diff: show changes

## suffix aliases
alias -s {css,html,js,md,sh,zsh,,}='code' # open file.{ext} in VSCode

# notes & references
## https://www.cyberciti.biz/tips/bash-aliases-mac-centos-linux-unix.html

## ^ 2022-01-12T21:16:44-0500
## % 2022-06-16T20:11:36-0500
